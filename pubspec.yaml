name: card_scanner
description: Card scanner
version: 0.0.1
repository: https://github.com/samihaddad/card_scanner/
issue_tracker: https://github.com/samihaddad/card_scanner/issues

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.10.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.5.8+5

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0

flutter:
  plugin:
    platforms:
      android:
        package: dev.samihaddad.card_scanner
        pluginClass: CardScannerPlugin
      ios:
        pluginClass: CardScannerPlugin
